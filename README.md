# 🚀 AI Chat Hotkey

Instantly launch and focus any AI chat interface with a single keystroke - no more app switching, 
no more bugs from bloated clients. Just your favorite AI, ready to chat.

## Building executable

```shell
uv run pyinstaller --onefile --name AIChatHotkey ai-chat-hotkey.py

```